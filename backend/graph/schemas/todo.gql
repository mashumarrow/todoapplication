type Todo {
  userid: ID!
  title: String!
  completed: Boolean!
  subject: Subject!
}

input NewTodo {
  title: String!
}
extend type Query {
  todos: [Todo!]!
  todo(userid: ID!): Todo!
}

extend type Mutation {
  createTodo(input: NewTodo!): Todo!
}
